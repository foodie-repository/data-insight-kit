# data-insight-kit DB 연결 설정 예시.
# 이 파일을 복사해 connectors/.env 로 저장하고 값을 채운다. (.env 는 절대 커밋 금지)

# DuckDB 데이터베이스 파일 경로 (로컬/외장 SSD).
# 외장 SSD 경로는 기기마다 다르므로 각자 환경에 맞게 설정.
DIK_DUCKDB_PATH=/path/to/analytics.duckdb

# 연결은 항상 read-only 로 열린다 (source.py 가 강제). 쓰기/DDL/DML 불가.
